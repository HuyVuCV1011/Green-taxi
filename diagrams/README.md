# Diagrams

Lưu file nguồn `.drawio` cùng ảnh PNG/SVG export. Mỗi sơ đồ phải khớp với ADR và
tài liệu kiến trúc hiện hành.

Sơ đồ triển khai phải phân biệt PostgreSQL nguồn Dispatch với PostgreSQL
warehouse, và phân biệt Google Drive release với source systems nghiệp vụ.
